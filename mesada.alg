algoritmo "mesada"

var
mesada, total: real

inicio
escreva ("Digite o valor da mesada recebida: ")
leia (mesada)
total <- mesada + 400
escreva ("O total guardado é de R$ ",total,",00")

fimalgoritmo
